# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# AI/ML Models
dashscope>=1.20.0
pywebview>=5.0.0

# Data Validation & Processing
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0
PyJWT>=2.8.0

# Cloud Services
alibabacloud-videoenhan20200320>=1.0.0
alibabacloud-tea-openapi>=0.3.0
alibabacloud-tea-util>=0.3.0
oss2>=2.18.0

# Audio Processing
demucs>=4.0.0
soundfile>=0.12.0
# 卡点的节拍检测直接 import numpy（src/apps/comic_gen/beats.py）。它此前只是
# 别的包的传递依赖，恰好装上了——列在这里是因为现在有代码明写着依赖它。
numpy>=1.24.0

# LLM 客户端。不是只有 LLM_PROVIDER=openai 才需要——默认的 DashScope 也是
# 通过阿里云的 OpenAI 兼容端点调用的，用的就是这个 SDK（见 llm_adapter.py 的
# _get_client）。缺它时剧本分析会直接失败，而报错只提 openai，很容易被误读成
# "我配了阿里云 key 为什么还要 OpenAI"。
openai>=1.0.0

# Optional Dependencies
# Uncomment if needed:
# pillow>=10.0.0
