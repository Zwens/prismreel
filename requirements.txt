# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# AI/ML Models
pywebview>=5.0.0

# Data Validation & Processing
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0
PyJWT>=2.8.0
# passlib 1.7.4（最後一版，已停止維護）透過已移除的 bcrypt.__about__.__version__
# 探測版本，bcrypt>=4.1 拿掉這個屬性後 passlib 的自我檢測會炸成
# "password cannot be longer than 72 bytes"，即使密碼很短也一樣。
# 釘住 4.0.1 是目前唯一可行的相容版本。
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# Cloud Services
alibabacloud-videoenhan20200320>=1.0.0
alibabacloud-tea-openapi>=0.3.0
alibabacloud-tea-util>=0.3.0
oss2>=2.18.0

# Audio Processing
demucs>=4.0.0
soundfile>=0.12.0
# 卡点的节拍检测直接 import numpy（src/apps/comic_gen/beats.py）。它此前只是
# 别的包的传递依赖，恰好装上了——列在这里是因为现在有代码明写着依赖它。
numpy>=1.24.0

# LLM 客户端。Gemini 通过其 OpenAI 兼容层复用同一个包
# 通过阿里云的 OpenAI 兼容端点调用的，用的就是这个 SDK（见 llm_adapter.py 的
# _get_client）。缺它时剧本分析会直接失败，而报错只提 openai，很容易被误读成
# "我配了阿里云 key 为什么还要 OpenAI"。
openai>=1.0.0

# 图像格式转码。Vidu 生图接口无论请求什么都只返回 JPEG（`format` 字段会被
# 静默忽略，已对线上 API 验证过），而本仓库所有生图调用方都按 .png 构造路径
# 并把该路径直接存进数据模型，所以 Vidu adapter 必须把字节转码成调用方要的
# 格式，见 src/models/vidu.py 的 _download_vidu_image。
pillow>=10.0.0
