# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# AI/ML Models
# LLM 客户端。Gemini 通过其 OpenAI 兼容层复用同一个包，见 llm_adapter.py 的
# _get_client。缺它时剧本分析会直接失败（RuntimeError），无 try/except 降级。
openai>=1.0.0

# 卡点的节拍检测直接 import numpy（src/apps/comic_gen/beats.py 模块顶层硬
# import），非降级依赖。
numpy>=1.24.0

# 图像格式转码。Vidu 生图接口只返回 JPEG，仓库所有生图调用方按 .png 构造路径，
# 见 src/models/vidu.py 的 _download_vidu_image。
pillow>=10.0.0

# BGM 人声分离（DUB 卡点功能）。缺失时 pipeline.py 已 try/except 优雅降级为
# 警告（"[DUB] Demucs model warmup failed"），非阻塞性依赖，但补上才能真正启用。
soundfile>=0.12.0

# Data Validation & Processing
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0
PyJWT>=2.8.0
# passlib 1.7.4（最後一版，已停止維護）透過已移除的 bcrypt.__about__.__version__
# 探測版本，bcrypt>=4.1 拿掉這個屬性後 passlib 的自我檢測會炸成
# "password cannot be longer than 72 bytes"，即使密碼很短也一樣。
# 釘住 4.0.1 是目前唯一可行的相容版本。
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# Cloud Services
alibabacloud-videoenhan20200320>=1.0.0
alibabacloud-tea-openapi>=0.3.0
alibabacloud-tea-util>=0.3.0
oss2>=2.18.0
